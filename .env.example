# Optional. Leave unset and the tracker stores data on the device only.
# Set both (Project Settings → API in Supabase) to make the tracker shared and live.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
